# OpenClaw Configuration Backup

此仓库包含 OpenClaw 的重要配置文件和工作空间。

## 内容说明

- `workspace-it/` - IT 工作空间（比尔·盖茨身份配置）
- `extensions/skills/` - OpenClaw 技能配置
- `identity/` - 身份相关配置
- `openclaw.json` - 主配置文件
- `cron/` - 定时任务配置
- `device-*.json` - 设备认证文件

## 排除的文件

敏感文件（密钥、凭证、日志）已排除。

---

*自动备份 - OpenClaw*
